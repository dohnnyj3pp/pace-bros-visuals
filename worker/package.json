{
  "name": "pace-bros-media-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run",
    "test": "node --test test/*.test.mjs"
  },
  "devDependencies": {
    "wrangler": "4.127.1"
  }
}
